

import 'package:flutter/material.dart';

class GameOfLifeHome extends StatelessWidget {
  const GameOfLifeHome({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
